#!/usr/bin/env sh

# Turbopack's build workers require local port binding, which is unavailable in
# some restricted environments. Webpack provides the same production checks here.
pnpm exec next build --webpack
pnpm run test
